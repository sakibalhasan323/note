# Qnote FRONTEND (static SPA - publish to InfinityFree or Netlify)
# Point the static frontend at your separately hosted backend API.
# Example: VITE_API_URL=https://your-api.onrender.com  (no trailing slash)
# Leave empty to use /api relative path.
VITE_API_URL=""
