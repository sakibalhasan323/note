Options -MultiViews
RewriteEngine On

# Let real files (built assets, favicon, uploads) pass through.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send SPA routes such as /admin to the built React entry point.
RewriteRule ^ index.html [L]