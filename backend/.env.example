# Qnote BACKEND API (Express - deploy to Render / Netlify / Railway / Heroku)

# Comma-separated list of allowed frontend origins. Use "*" for any origin.
CORS_ORIGINS="*"

# Admin login (uses these if set; otherwise falls back to data/admin_account.json)
ADMIN_USERNAME="admin@notes.local"
ADMIN_PASSWORD="admin123"

# Firebase web configuration (overrides data/website_settings.json if set)
FIREBASE_API_KEY="AIzaSyBw1vhpCHelRkv8BMnCcemQ-50xZyMCdc0"
FIREBASE_AUTH_DOMAIN="note-site-je.firebaseapp.com"
FIREBASE_PROJECT_ID="note-site-je"
FIREBASE_STORAGE_BUCKET="note-site-je.firebasestorage.app"
FIREBASE_MESSAGING_SENDER_ID="1011717430011"
FIREBASE_APP_ID="1:1011717430011:web:ec41c01ffc09014d422e5e"

# Backend port (hosting platforms provide this automatically)
PORT="3000"
